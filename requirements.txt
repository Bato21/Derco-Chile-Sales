# Business Intelligence Final Project — DERCO Chile
# Course stack: Python + pandas + scikit-learn + matplotlib
# Install:  pip install -r requirements.txt

# --- notebook (Stages 1-5) ---
pandas>=2.0
numpy>=1.24
scikit-learn>=1.3       # HistGradientBoostingClassifier(class_weight=...) needs >=1.4
matplotlib>=3.7
openpyxl>=3.1           # reads the source .xlsx
pyarrow>=14.0           # writes the clean .parquet
jupyter>=1.0
nbformat>=5.9
nbconvert>=7.0          # "Restart & Run All" from the command line

# --- presentation ---
python-pptx>=1.0        # presentation/build_pptx.py -> BI2026_FinalProject_DERCO.pptx
